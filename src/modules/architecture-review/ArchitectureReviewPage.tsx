import { getPresentationContent } from "@/lib/content";
import { ItemGrid, NumberList, PresentationSlide, PresentationSlideBody, PresentationSlideFooter, PresentationSlideHeader } from "@/components/presentation/PresentationPrimitives";
import { InlineInfoDialog } from "@/components/presentation/InlineInfoDialog";

export function ArchitectureReviewPage() {
  const { architecture } = getPresentationContent();
  return <PresentationSlide><PresentationSlideHeader eyebrow="Parte 1 · Arquitectura" title="Revisión de arquitectura InflightOS" thesis={architecture.summary} actions={<InlineInfoDialog eyebrow="Arquitectura" id="architecture-detail-dialog" title="Evidencia completa"><div className="dialog-grid"><div className="dialog-panel"><h3>5 fortalezas</h3><ItemGrid items={architecture.strengths} columns={3} /></div><div className="dialog-panel"><h3>5 riesgos</h3><NumberList items={architecture.risks} /></div><div className="dialog-panel"><h3>3 áreas diferidas</h3><ItemGrid items={architecture.deferred} columns={3} /></div><div className="dialog-panel"><h3>Pila de prioridades</h3><NumberList items={architecture.priorities} /></div></div></InlineInfoDialog>} /><PresentationSlideBody><div className="architecture-decision"><section className="decision-card primary"><span className="decision-label">Decisión de forma</span><h2>Monolito modular primero. <span className="accent">Servicios extraídos por evidencia.</span></h2><p>{architecture.summary}</p><div className="decision-pills"><span>Bounded contexts</span><span>Outbox</span><span>Scopes</span><span>Gates</span></div></section><section className="compare-grid">{architecture.strengths.slice(0, 3).map((item, index) => <article className="compare-card" key={item.id}><span className="badge info">0{index + 1}</span><h3>{item.title}</h3><p>{item.description}</p></article>)}</section></div></PresentationSlideBody><PresentationSlideFooter><span>Idea dominante</span><span>Arquitectura seria significa foco, ownership y salida por evidencia.</span><InlineInfoDialog buttonLabel="Riesgos" eyebrow="Risk register" id="architecture-risk-dialog" title="Registro de riesgos"><NumberList items={architecture.risks} /></InlineInfoDialog></PresentationSlideFooter></PresentationSlide>;
}
